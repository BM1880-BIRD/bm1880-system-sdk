diff --git a/drivers/spi/spi-dw.c b/drivers/spi/spi-dw.c
index 27960e4..877df7e 100644
--- a/drivers/spi/spi-dw.c
+++ b/drivers/spi/spi-dw.c
@@ -368,6 +368,8 @@ static int dw_spi_transfer_one(struct spi_master *master,
 		dws->transfer_handler = interrupt_transfer;
 	}
 
+	dw_spi_set_cs(spi, 0); /* add temporary to do fixed chip select */
+
 	spi_enable_chip(dws, 1);
 
 	if (dws->dma_mapped) {
